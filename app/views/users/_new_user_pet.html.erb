<% if current_user == @user %>
	<div class="col-md-2 col-md-offset-10" id="form-btn">
		<%= link_to 'Add Pet', new_pet_path, class: "link-btn", id: "userPet", remote: true %>
		</div>
	<div class="col-md-6 col-md-offset-3" id="newPet">
		<%= render 'pets/form' %>
	</div>
<% end %>
